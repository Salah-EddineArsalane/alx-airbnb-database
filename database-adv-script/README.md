**INNER JOIN**: show all bookings with the users who made them.

**LEFT JOIN**: show all properties with their reviews (include properties without reviews).

**FULL OUTER JOIN**: show all users and all bookings (even if unmatched).
